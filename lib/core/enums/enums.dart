enum BottomNavTypeSelected {
  logOut,
  search,
  newSearch,
  editProfile,
  newSearchDownload,
}

enum WorkerRegisterPage { newWorker, existsDatabase, existsProject }

enum WorkerRegisterNotify { registeredSuccess, assignedTo, nameIdUpdated }

enum WorkerPhotoCaptureTextType { clickCapture, captureSuccessful}

enum ExceptionType { response, timeout, server,unknown}
